const fs = require('fs');

// const filename = Math.round(Math.random() * 1000);
// const filename = "info";
// const rename = "information";
// const folderName = 'it-department';

// =========== Make Directory ===============
// fs.mkdirSync(folderName);

// =========== Write Content In The File ===============
// fs.writeFileSync(`it-department/${filename}.txt`, 'This is my first written file.');

// =========== Append Content In The File ===============
// fs.appendFileSync(`it-department/${filename}.txt`, 'This is my first written file.');

// =========== Rename The File Name ===============
// fs.renameSync(`it-department/${filename}.txt`, `it-department/${rename}.txt`)

// =========== Read Content From The File ===============
// const data = fs.readFileSync(`it-department/${rename}.txt`, 'utf-8');
// console.log(data);

// =========== Delete The File ===============
// fs.unlinkSync(`it-department/${rename}.txt`);

// =========== Delete The Directory ===============
// fs.rmdirSync(folderName);
